# Required: Web App URL only (must end with /exec), from Apps Script → Deploy → Web app.
# Wrong URLs (403): script editor links containing /macros/edit — see scripts/google-apps-script-RegisterWebhook.gs
GOOGLE_SHEETS_WEBHOOK_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
